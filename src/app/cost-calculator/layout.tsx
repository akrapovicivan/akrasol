export default function CostCalculatorLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <h2>Cost Calculator</h2>
            {children}
        </div>
    );
}
