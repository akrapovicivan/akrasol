export default function CurrentDiscountsLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <h2>CurrentDiscounts</h2>
            {children}
        </div>
    );
}
