export default function PackageDetailsLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <h2>PackageDetails</h2>
            {children}
        </div>
    );
}
