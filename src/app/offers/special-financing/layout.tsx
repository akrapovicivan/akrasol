export default function SpecialFinancingLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <h2>SpecialFinancing</h2>
            {children}
        </div>
    );
}
